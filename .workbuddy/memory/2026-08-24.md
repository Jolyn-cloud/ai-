# 2026-08-24 工作日志

## 闪卡单卡模式二级页面完成
- 新建 `05_UI设计/闪卡单卡模式.html`，实现闪卡功能规则.md §3 单卡模式全部交互
- 核心交互：3D翻卡（点击翻转）+ 左右滑动判定（右滑已记住/左滑未记住）
- 强约束实现：未翻转时不可滑动，翻转后才能滑动判定
- 滑动视觉反馈：绿色"已记住"/红色"未记住"遮罩，松手才提交
- 艾宾浩斯规则：未记住卡塞回队列末尾当天重现，累计未记住≥3次打顽固卡标记
- 12张演示卡覆盖10章真实考点，完成结算页展示已记住/未记住/顽固卡统计
- 更新 `闪卡首页.html` continueStudy() 从 alert 改为跳转到单卡模式页面
- 设计Token与首页完全一致（渐变背景、气泡氛围、青蓝主色、圆角等）

## 闪卡单卡模式二次迭代（PM 反馈）
- 底部新增实时统计条：已记住 N / 未记住 N / 顽固 N（三色圆点 pill，绿/红/橙）
- 新增"拖到侧边立即判定"：拖动距离 ≥170px（约卡片区 50%）无需松手即提交，未到侧边松手仍按 80px 阈值判定；autoCommitted 标志防重复提交
- 卡片章节标签改为"一级分类-二级分类"（如 信息与计算机基础知识-数制与数据编码），sub 字段与首页 CHAPTERS.subs 二级命名对齐
- 顽固卡判定统一为累计未记住 ≥3 次（原演示代码为 ≥2），与规则文档 §2.1 一致
- 同步更新 `闪卡功能规则.md` §3.2/§3.3/变更记录

## 列表模式 + 双模式数据共享（PM 需求：增加列表模式并完善所有交互）
- 新建共享数据层 `05_UI设计/闪卡题库.js`：CARDS 卡池（含 forgotCount 跨天累计预设）+ 当日队列状态（localStorage 按天隔离，key=flashCardState_v1）
- 数据层 API：loadState/saveState/newState/buildQueue/markCard/calcStats/isStubbornCard/findCard
- 队列元素带唯一 key（c0#1），未记住副本塞回队尾 round+1（当天重现），同 id forgotCount 同步（顽固判定口径）
- 新建 `05_UI设计/闪卡列表模式.html`：未答在上、已答置灰折叠在下；点击遮挡区原位展开答案+解析（max-height 动画）；未展开按钮禁用；已答卡可展开只看答案不可重复标记；未记住副本带「今日重现」角标；底部实时统计条；全部完成进结算页；顶栏「切到单卡」入口
- 改造 `闪卡单卡模式.html`：移除内嵌 CARDS → 引用闪卡题库.js；findNextPending 跳过当日已标记卡（中断续刷）；统计全部走 calcStats；顽固判定改 isStubbornCard；卡片补「今日重现」角标；顶栏加「切到列表」入口；restart 生成全新当天状态
- 改造 `闪卡首页.html`：继续背诵 → 底部模式选择弹层（单卡/列表 + 今日已刷进度，数据来自共享层）
- node 全量校验：3 页面 + 数据层 JS 语法 OK，无旧变量残留（CARDS/queue[currentIndex]/cardForgotMap 等已清除）
- 数据层行为测试通过：未记住副本 round+1、同 id forgotCount 同步、顽固卡（预设 2 张 forgotCount=3）正确识别、双模式共享状态一致
- 同步更新 `闪卡功能规则.md` §4.5（已实现交互细节）+ 变更记录

## 模式切换简化（PM 反馈：不需要选择背诵模式）
- `闪卡首页.html`：移除模式选择底部弹层（HTML/CSS/JS 全部删除），「继续背诵」直接跳转 `闪卡单卡模式.html`（默认单卡模式）
- 模式切换只保留页面右上角按钮：单卡↔列表互切（goList/goSingle），数据仍走共享层 `闪卡题库.js`，进度跨模式一致
- 首页 `<script src="闪卡题库.js">` 引用保留（无副作用，供后续接入真实进度）
- node 校验通过：三页面 JS 语法 OK、无 modeSheet/goMode/sheet 残留、双模式互切链接齐全

## 交互修复（PM 反馈两处问题）
- 单卡模式：删除冗余「操作按钮区」（btnForgot/btnRemembered + .actions CSS + JS 绑定 + updateUI 按钮逻辑），翻卡后仅保留 swipe-hints 滑动方向提示 + 底部统计条，消除"下方重复出现两个未记住/已记住标记"
- 列表模式：修复标记后滚动定位 —— 原 `scrollTop=scrollHeight` 直接甩到底部（已答区），改为记录被标记卡在未答区的序号与 offsetTop，渲染后把「下一张未答卡」平移到原卡位置（scrollTop 补偿），连续刷题不跳屏；标记最后一张时自动 clamp
- node 校验通过：两页面 JS 语法 OK、无按钮残留、无 scrollHeight 甩底逻辑

## 最终收尾（任务8-12：标签左对齐 + 删重复提示 + 个性化配置）
- `闪卡单卡模式.html`：标签区左对齐分两行 —— 新增 .card-tags 容器（flex column / align-items:flex-start），第一行题型标签「一级-二级」，第二行卡片类型标签（顽固卡 / 今日重现），删除页底 swipe-hints 滑动提示条（卡片背面已有方向提示 + 滑动红绿遮罩），单卡模式回归纯滑动判定
- `闪卡题库.js` 新增配置能力：CONFIG_KEY=flashCardConfig_v1 存 {dailyCount, weights}；loadConfig/saveConfig/getDailyCount；buildQueue 三重优先级（顽固卡必现 → 按题型比例抽新卡 → 兜底补齐）；getDailyCount=min(配置值, 卡池数) 收缩；saveConfig 当天 done=0 时重建队列立即生效、已开始次日生效；dailyCount=0 视为无效回退默认 350
- 新建 `闪卡设置.html`（背诵设置页）：每日数量滑杆 10~500 + 快捷 chips（30/50/100/200/350）+ 题型比例 10 章滑杆（0~100% 自动折算、均分/清空按钮）+ 底部固定保存栏 + 生效时机提示；range 滑杆用 CSS 变量 --fill 做选中段渐变填充
- `闪卡首页.html`：背诵计划卡副标题动态显示「每日 N 张」（render 时读 getDailyCount），右上角白色齿轮按钮进入设置页
- `闪卡功能规则.md`：§1.2 每日配额（默认 350、可自定义、队列优先级、实际数量收缩规则）；§3.2 强约束新增「不设页底滑动提示条」；§3.3 页面元素改「已刷/每日配额」+ 标签左对齐分两行；变更记录 +2 条
- node 校验：4 页面 JS 语法 + 关键点全部通过
- 数据层行为测试 19 项全部通过：默认收缩 12、dailyCount=10 生效、单章权重 100% 入队、顽固卡 2 张必现（即使配额 5 且权重指向其他章也不被挤出）、saveConfig done=0 立即重建/done>0 保留进度、markCard 副本 round+1、边界（0→回退默认、500→收缩 12）

## 小修复（PM 反馈：设置页清空/均分按钮重复）
- `闪卡设置.html`：删除「清空（默认均分）」按钮与 clearWeights() 函数 —— 因数据层对空 weights 的兜底行为就是各章均分，与「均分（各 10%）」最终效果完全一致，属冗余按钮；保留「一键均分」并更新 desc 说明「不设置时默认各章节均分」
- 校验：设置页 JS 语法 OK，无 clearWeights/清空按钮残留

## 比例语义重构（PM 反馈：80+50 超 100 困惑）
- 问题：滑杆标「%」但实为相对权重（80+50 归一化为 80/130、50/130），界面合计显示 130% 误导
- 修复（闪卡设置.html）：改为「比例锁」——各章合计恒为 100%，拖动某章时其余章节按 k=(100-v)/其他合计 等比压缩，末项兜底调平；滑杆 step 5→1；每章今日张数 = 每日数量 × 该章比例；renderWeights 对老数据（合计≠100）自动归一化渲染；合计行提示改「合计恒为 100% · 每章今日张数 = 每日数量 × 该章比例」
- 数据层（闪卡题库.js）无需改动：totalW=100 时归一化=直接百分比，80%→80 张（配额 100）；合计 130 的旧权重数据层兜底归一化不超配额
- 规则文档 §1.2 同步「比例锁」语义
- 测试：数据层 25 项全过（新增 80/20 百分比分配、合计 130 归一化兜底）；设置页 jsdom 联动测试 17 项全过（初始 100、拖 80 压缩、拖 100 清空、交叉拖动、旧权重归一化 62/38、保存收集合计 100）——jsdom 需 beforeParse 注入 localStorage mock（file:// 源无原生 localStorage）

## 章节统计树形详情页（PM 参考截图）
- 新建 `闪卡章节统计.html`：接收 `?chapter=章节名`，顶部章节头部卡片（总卡片/已记住/未记住/掌握%），下方树形列表
- 树形结构：二级（sub）点击展开三级（topic），三级点击展开四级（point）；四级节点显示「N 张  ✓对  ✕错」，对数绿色、错数红色
- 数据层（`闪卡题库.js`）：
  - 给部分演示卡片新增可选 `topic`/`point` 字段
  - 新增 `getChapterStats(state, chapterName)`：按 chapter → sub → topic → point 聚合 total/right/wrong
  - 新增 `mergeRealStats` 用于把真实统计合并到演示树（本次页面内独立实现 `mergeReal` 更灵活，后续可下沉）
- 首页（`闪卡首页.html`）：二级 `.sub-item` 增加 `onclick="goChapterDetail(id)"` 和右箭头 ›，点击进入章节统计页
- 规则文档 `闪卡功能规则.md`：§6.1 二级行增加入口说明；新增 §6.2 章节统计详情页规则；变更记录 +1 条
- 验证：4 个文件 JS 语法通过；章节统计页 jsdom 测试 14 项全过（树形渲染、展开/收起、对数/错数显示、真实答题状态聚合）

## 小程序主题背景统一（PM 提供淡紫梦幻背景图）
- 将 `/Users/a825/Desktop/专升本/ChatGPT Image 2026年8月24日 18_17_28.png` 复制到 `05_UI设计/bg-flashcard-home.png`，作为闪卡小程序全模块主题背景
- `闪卡首页.html`：.page 背景改为 `url('bg-flashcard-home.png') center top / cover no-repeat`；删除 CSS 绘制泡泡与点阵装饰；保留 bottom-mask 白色渐隐遮罩；HTML 移除 white-bubble/dot-pattern/bubble 元素
- 二级页面统一复用背景图 + bottom-mask：
  - `闪卡单卡模式.html`：替换渐变背景，删除 bubble 元素，新增 bottom-mask
  - `闪卡列表模式.html`：同上
  - `闪卡设置.html`：同上
  - `闪卡章节统计.html`：同上（保留 --bg-bottom 作为兜底）
- `设计Token.md` 同步：页面背景改为图片背景实现，bottom-mask 为保留氛围+下方干净白色的过渡
- `闪卡功能规则.md` 变更记录 +1 条
- 验证：5 个页面 + 题库 JS 语法全部通过

## 固定手机比例与壳内滚动（PM：手机真实使用感受）
- 全部 5 个闪卡页面统一改造：
  - `.page` 改为 `height: 100vh; display:flex; flex-direction:column; overflow:hidden`
  - `.content` 改为 `flex:1; min-height:0; overflow-y:auto; -webkit-overflow-scrolling:touch; scrollbar-width:none; ::-webkit-scrollbar{display:none}`
  - 桌面端（`@media (min-width: 481px)`）居中显示 390×844 手机壳：圆角 34px、深色边框 bezel、外阴影、灰色渐变背景
  - 真机（≤480px）.page 宽度 100%、无圆角边框、全屏
- 特殊处理：
  - `闪卡设置.html` save-bar 从 `position:fixed` 改为 `position:absolute`，使其贴附在手机壳底部而非浏览器窗口
  - 单卡/列表页 body 原本 `overflow:hidden`，媒体查询中增加 `html,body { height:100%; overflow:hidden }` 确保壳体垂直居中
- 验证：
  - 5 页面 JS 语法全部通过
  - Playwright 桌面截图（1280×900）：首页/设置页正确渲染为居中圆角手机
  - Playwright 移动端截图（390×844）：首页全屏无壳，内容在手机屏幕内滚动

## 替换首页背景图
- 将 PM 提供的新背景图 `ChatGPT Image 2026年8月24日 18_39_38.png` 复制替换 `05_UI设计/bg-flashcard-home.png`
- 5 个闪卡页面继续统一引用同一背景图，无需改代码
- Playwright 桌面截图验证：首页在手机壳内正确渲染，新背景淡紫色+右上光晕+左下点阵与玻璃卡片融合良好

## 章节统计页修复（PM：背景换淡紫 + 间距 + 内容淡出 bug）
- 背景独立化：`闪卡章节统计.html` `.page` 不再引用统一 `bg-flashcard-home.png`，改为淡紫渐变（#E9E7F8 → #EDEBFA → #F2F1FB → #F5F5FC）
- 删除 `bottom-mask` 白色渐隐遮罩元素及对应 CSS（含第 86-94 行无选择器孤儿 CSS 块）——该遮罩+孤儿块导致「章节掌握详情」列表下半屏被白雾罩淡、无法查看
- 增加呼吸间距：`.content` padding-bottom 从 30px 改为 34px，底部卡片不贴手机壳边缘
- 校验：无 `bg-flashcard-home`/`bottom-mask` 残留，JS 语法通过
- Playwright 验证：淡紫背景正常，列表内容清晰无白雾淡出（展开的三级/四级节点通过元素高度检测确认渲染：sub3ListH=89px，maxHeight=3000px）

## 19:05 闪卡模块发布线上预览
- 将 05_UI设计 整目录（5 个 HTML + 闪卡题库.js + 背景图）部署为静态站点，入口 闪卡首页.html
- 分享链接：https://f172f27a12db4ec2b4d2f45feef6ee33.app.workbuddy.link（用于发客户预览）
- 页面间相对路径跳转 + localStorage 在 https 下正常

## 20:55 闪卡模块迭代：填空题 + 章节统计进入闪卡
- 闪卡题库.js：12 张卡片统一改为填空题题干（带 ____），补充 topic/point，buildQueue 透传字段；新增 clozeHtml、filterState、matchesFilter、calcStatsFiltered，升级 FLASH_KEY 到 v2
- 闪卡单卡模式.html：正面渲染填空空位，背面显示答案高亮填入；新增「填空题」标签；支持 URL 过滤（chapter/sub/topic/point），标记写回 baseState 并刷新过滤视图；模式切换保留过滤参数
- 闪卡列表模式.html：列表题目同样渲染填空空位；新增「填空题」标签；支持过滤与参数透传
- 闪卡章节统计.html：一级 hero、二级/三级/四级标题均可点击进入对应范围闪卡；箭头仍负责展开/收起
- 闪卡功能规则.md：同步填空题规则、章节统计点击进入闪卡、变更记录
- 验证：Node 语法检查通过；Playwright 移动截图确认单卡/列表/章节统计/过滤均正常
- 重新部署线上预览：链接不变

## 21:00 闪卡单卡模式新增「上一张」按钮
- 闪卡单卡模式.html：底部卡片区与统计条之间增加「上一张」按钮；实现 historyStack 撤销栈
- 点击「上一张」时撤销最近一次标记：删除 baseState.marked[key]；若原为「未记住」，移除 markCard 新增的副本并恢复 forgotCount
- 重新初始化/再背一轮时清空历史栈；无历史记录时按钮自动禁用
- 验证：Node 语法检查通过；Playwright 移动截图确认按钮禁用→标记后启用→撤销后禁用、进度与卡片正确回退
- 重新部署线上预览：链接不变

## 21:20 闪卡单卡模式：完成页指标增强 + 顽固卡符号化
- 按 `electric-vortex-darwin.md` 计划改造 `闪卡单卡模式.html`（数据层 `闪卡题库.js` 已预先扩展，本次只改 HTML 页面）
- **计时启动**：`init()` 增加 `if (!getSessionStart()) startSession()` 防重复 guard；`restart()` 先 `clearSessionStart()` 再走 init
- **顽固卡符号化**：移除 `.stubborn-tag` 胶囊标签 DOM 输出，改为 `.stubborn-corner` 🔥 图标（24×24px 圆形，#F28B3D），注入正/背两面 card-face 右上角，保留 title/aria-label
- **完成页 HTML 重构**：替换旧的三格统计为六区块布局 —— hero（emoji+动态标题+范围副标题）→ 环形图核心指标（SVG 环形正确率 + 总张数/已记住/未记住）→ 薄弱知识点 TOP5（序号+名称+路径+进度条+错误次数）→ 章节掌握度（绿/红堆叠条）→ 效率分析（本次用时/平均速度/较上次↑↓%）→ 巩固 CTA（立即练习跳转列表模式最薄弱章节）→ 底部操作（返回首页/再背一轮，有章节过滤时增「下一章」）
- **showCompletion() 重写**：计算 stats/elapsed/speed/accuracy/weakPoints/chapterDist/lastHistory，全量渲染各区块；环形图 strokeDashoffset 动画；动态标题按正确率分级（≥80%🎉不错哦 / ≥60%💪继续加油 / <60%📚再加把劲）；调用 recordHistory 写入历史、clearSessionStart 清除计时
- 新增辅助函数：getRangeText()、formatDuration()、goToPractice()、goNextChapter()
- 验证：Node 语法检查通过（HTML 内联 JS + 闪卡题库.js）；grep 确认旧元素 ID（statRemembered/stats-grid/completion-icon/stubborn-tag DOM）已全部移除

## 21:32 单卡模式标签精简（PM 反馈）
- 移除「今日重现」角标：删除 .round-tag CSS + renderCard 中 card.round>1 的 DOM 输出（未记住副本不再有视觉标记，功能本身保留）
- 移除「填空题」标签：删除 .fillin-tag CSS + DOM 输出（全部卡均为填空题，标记无信息量）；保留 cloze-blank/cloze-fill 题干空位样式
- 顽固卡右上角标记改为「线性警示三角 SVG + 顽固 文字」胶囊徽标（橙色 #F28B3D / warning-light 底），替换原 🔥 emoji（语义不清）；正/背两面 card-face 均注入
- 同步删除遗留的 .stubborn-tag CSS 与 pulse-tag 动画
- 验证：Node 语法检查通过；grep 无 fillin-tag/round-tag/今日重现/🔥 残留

## 21:36 单卡模式三 bug 修复（PM 反馈）
- **bug1 翻卡后顽固标记重复**：根因是 `.stubborn-corner` 的 `z-index:5` 创建独立层叠上下文，在 preserve-3d 结构中逃逸父级 `.card-face` 的 `backface-visibility:hidden`（正/背面角标同显）。修复：删除 z-index + 角标自身加 backface-visibility:hidden。经验：3D 翻卡组件内所有绝对定位子元素都不可带 z-index（标签没重复正是因无 z-index）
- **bug2 已刷 12/12 不出成果页**：原完成判定是「队列耗尽」，但未记住副本塞回队尾导致 done 到达配额时副本仍在队列、永不触发。修复：commitCard 的 setTimeout 和 init() 均增加 `calcStats(state).done >= total` 判定（与列表模式/进度条口径对齐）；init 防重进
- **bug3 点列表按钮直接变成果页**：bug2 连带——列表页本就用 done>=total 判定，单卡模式未对齐导致认知不一致，bug2 修复后消失
- **历史重复记录**：showCompletion 每次执行都写 recordHistory，刷新/重进会重复。修复：内存 flag `historyRecorded` + 持久化标记 `flashCardResultRecorded_v1`（date#done），restart/goPrev 撤销时清除
- 验证：Playwright 真实浏览器 13 项全过——角标 DOM=2 且 zIndex=auto、左滑副本入队（queue 13）、done=12 时 pending 副本仍在 + 成果页正确弹出、刷新不重复记录历史、restart 归零、未完成切列表正常
- 测试陷阱记录：Playwright mouse 拖动后页面 `hasMoved` 残留会吞掉下一轮 click（防误翻逻辑），每轮需 evaluate 重置；连续两次 click 会翻回正面导致拖动失效

## 21:55 列表模式完成页对齐单卡学习成果页（PM 需求）
- `闪卡列表模式.html` 完成页从旧三格统计（已记住/未记住/顽固卡）重构为与单卡模式完全一致的六区块学习成果页：hero（emoji+动态标题+范围副标题）→ 环形正确率+三指标 → 薄弱知识点TOP5 → 章节掌握度 → 效率分析（用时/速度/较上次）→ 巩固CTA → 底部操作（有章节过滤时增「下一章」）
- 新增：启动计时 `if (!getSessionStart()) startSession()`（与单卡共享 SESSION_START_KEY，跨模式续时）；showCompletion() 全量渲染 + recordHistory 防重复（RESULT_RECORDED_KEY 同款守卫）；辅助函数 getRangeText/formatDuration/goToPractice/goNextChapter
- 导航适配：列表模式 goToPractice 跳「单卡模式?chapter=最薄弱章」（与单卡模式跳列表方向互为镜像）；goNextChapter 留在列表模式
- restart() 补充 clearSessionStart/historyRecorded/clearResultRecorded
- 验证：Node 语法通过；Playwright 17/17 全过（全流程标记→成果页六区块渲染→刷新历史不重复→再背一轮归零）+ 章节过滤场景 6/6（副标题显示范围、100%动态标题、下一章按钮出现、历史1条）
