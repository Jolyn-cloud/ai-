# 2026-08-25 工作日志

## 闪卡首页：设置按钮改为每日数量浮窗
- 文件：`05_UI设计/闪卡首页.html`（唯一改动文件）
- 需求："连续 7 天"旁齿轮按钮，由跳转 `闪卡设置.html` 改为点击弹出按钮下方浮窗，仅设置每日学习卡片数量（滑杆 10~500 step10 + 快捷 chips 30/50/100/200/350）
- 生效逻辑保持：调用 `闪卡题库.js` 的 `saveConfig()`（未开始刷→立即生效；已开始→次日生效），浮窗内绿色/橙色提示条反馈生效方式，1.8s 后自动关闭
- 实现要点：
  - `.study-card` 原有 `overflow:hidden` 会裁剪浮窗 → 打开时加 `.pop-open` 临时放开
  - 保存时 `loadConfig()` 读取现有 weights 一并传回，避免覆盖题型比例
  - `#dailyLabel` 保存后即时同步（`getDailyCount()` 会按卡池自动收缩）
  - 删除 `goSettings()`，点击浮窗外自动关闭
- 已确认：`闪卡设置.html` 文件保留但首页不再有入口（用户选择"完全替换"）
- 内联 JS 语法校验通过（node new Function）

## 修正：浮窗改为底部上滑弹窗（bottom sheet）
- 用户澄清交互：非按钮下方 popover，而是从手机底部上滑弹出 + 全屏半透明遮罩
- 实现：`.sheet-mask`（absolute inset:0 遮罩，点击关闭）+ `.settings-sheet`（absolute 贴 .page 底部，translateY(105%)↔0 上滑动画，cubic-bezier(.32,.72,.28,1)）
- 弹窗作为 `.page` 直接子元素（430px 手机壳容器内 absolute 定位，桌面预览时严格贴合手机屏）
- 结构：顶部把手条 + 标题行（含 × 关闭按钮）+ 数值/滑杆/chips/保存/提示条
- 移除 popover 版残留（settingsPop、pop-open、document 外部点击关闭监听），遮罩点击即关闭

## 首页：三/四级标题层级与交互调整（局部 UI 修改）
- 三级：17px/600/#5571E8，"›"三角 rotate(90deg) 0.2s 展开动画，仅"三角+文字"无右侧内容（原有结构已满足，仅调 CSS）
- 四级：15px/400/#68748D，7px 圆点 #C7CDD9，行高 48px，比三级右缩进
- 四级右侧状态改为三选一：绿色✓（mastered>0）/ 浅灰○（未掌握）/ 橙色圆角"顽固"标签（#FFF2E8/#FF8A45）
- 顽固判定：CARDS 中 forgotCount>=3 且 point===四级名（与闪卡题库.js 口径一致）
- 一/二级字号原为 15/13px，与要求的 22/18>17>15 层级冲突 → 按需求第五条仅调字号（22px/18px），内容布局右侧信息未动
- 删除旧 level4-status（✓N ✕N 计数）相关 CSS

## 回调：一/二级恢复原字号，三四级相应压缩
- 用户决定保持原一/二级字号（15px/13px），仅压缩三四级保证 15>13>三级>四级
- 最终字号：一级 15px / 二级 13px / 三级 12.5px（600, #5571E8）/ 四级 11.5px（400, #68748D）
- 配套缩小：三级三角 17→13px（含 16→13px 容器），四级 ✓/○ 16→14px，顽固标签 11→10.5px
- 其余（三角旋转动画、圆点、缩进、48px 行高、状态三选一）均未动

## 全部章节：四级层级视觉区分度优化（局部 UI）
- 二级：sub-item 加浅色背景 rgba(238,241,250,.55) + 圆角 10px 轻微区分；sub-title/sub-dot 字色改深青 #1A8FA3（不脱离冷色主题）
- 三级：新增 .l3-block 包裹「三级item + 四级list」，1px rgba(91,114,245,.09) 浅边框 + radius10 + overflow:hidden，建立层级不做明显卡片；level3-item 右侧新增 .level3-right 显示「已答/总数 .l3-mem + 百分比 .l3-pct」（取 l3.memorized/cards/done）
- 四级：level4-item 改独立轻量圆角框（1px rgba(103,116,141,.14) border + radius8 + 半透明白底），高度 48→40px，gap 5px 间距调小；缩进 margin-left:20px 略次于三级
- 四级右侧符号化：顽固>N 顽固标签（计数 forgotCount>=3）/ 全掌握>✓ / 否则 N×（N=unmastered）；删除旧 l4-circle ○
- level3-inner 缩进 30→22px，gap 5px；toggleLevel3/syncAncestors 在 l3-block 包裹下兄弟查找仍成立（item.nextElementSibling=level4-list）
- 语法校验通过，l4-circle 残留 0

## 二级字色微调 + 嵌套式层级容器改造（16:30~16:37）
- 二级 sub-title/sub-dot 字色：#1A8FA3（深青）→ #2f489a（深蓝），标题与圆点统一
- 新增 `.sub-container`（#FAFBFF 极浅底 + 1px rgba(91,114,245,.05) 隐约边框 + 12px 圆角 + 13px padding），JS 把 sub-item+sub-progress+level3-list 包进容器；sub-item 去掉蓝灰背景 rgba(238,241,250,.55) 改透明，sub-inner 去 border-top 改 gap:12px
- `.l3-block` 改白底嵌套容器（padding 9px 10px，边框 .07 更淡），level3-inner 去 margin-left 与二级左对齐，gap:7px
- level4-item 高度 42px 白底，level4-inner margin-left 12px / padding-top 8px / gap 7px
- 四级右侧改 ✓N / ×N / 顽固N 三者同时显示（各自 count>0 才渲染），不再是三选一
- 手风琴 toggleLevel2（nextElementSibling 跳过 sub-progress 找 level3-list）/ toggleLevel3 / syncAncestors 在容器包裹下逻辑仍成立，语法校验通过

## 二级进度条弱化（16:47）
- sub-progress：高度 3→2px，底轨 #e7eaf3→#EEF0F7，margin-top 4→6px（与二级标题间距 6~7px），下边距保持 11px
- sub-progress-inner：蓝色渐变改 rgba(91,114,245,0.40) 单色（约 40% 透明度），视觉降级为轻分隔线，不抢三/四级层级

## 二级收起间距 + 进度条贴合修复（16:50）
- 收起状态底部过宽：sub-progress margin-bottom 11→4px；展开时与三级列表的间隔改为 .level3-inner padding-top:8px（收起时随 max-height:0 隐藏，不影响收起紧凑度）
- 一级进度条与首个二级容器贴合：.sub-inner 加 padding-top:12px（sub-inner 在 sub-list 折叠容器内，仅一级展开时生效）
- 手风琴高度走 scrollHeight 计算，自动适配新间距，无需改 JS

## 四级答题情况常驻显示（16:55）
- 四级右侧由「各自>0 才显示」改为 ✓对N / ×错N / 顽固N 三项常驻显示（含 0），每个知识点都能看到答题情况
- 数据口径不变：mastered/unmastered 取自 l4 字段，顽固按 CARDS.forgotCount>=3 且 point 匹配计数；语法校验通过

## 列表模式答案遮挡样式调整（17:24 → 已回滚 17:27）
- 曾将遮挡块改为黄色 #ffe573、答案区改为绿底绿字，用户不满意，已全部回滚至修改前状态
- 当前 .lc-mask 恢复为浅蓝紫渐变 + 虚线边框，.lc-a-text 恢复为深色字无背景

## 列表模式填空题原位遮挡交互（17:31）
- 文件：`05_UI设计/闪卡列表模式.html`（CSS + JS，未改题库 JS）
- 新增 `clozeMaskHtml(q, answer)`：题干 `____` 替换为行内 `.cloze-mask` 可点击遮挡块（眼睛图标+"点击查看"）
- 新增 `revealClozeAnswer(cardEl, maskEl)`：遮挡块 `replaceWith` → `.cloze-revealed` 绿色答案文字 + 同步展开 `.lc-answer` 解析区 + 启用操作按钮
- cardHtml 改造：待答题用 clozeMaskHtml 渲染题干（去掉旧 .lc-mask 块），已答题用 clozeHtml(q, answer) 直接展示答案
- 答案区 `.lc-answer-inner` 只保留解析内容（去掉旧"答案"标签+答案文本，答案已原位揭示）
- 事件委托 .lc-mask → .cloze-mask，openAnswer → revealClozeAnswer
- 旧 openAnswer / .lc-mask CSS 为死代码（不再渲染/调用），不影响功能；语法校验通过

## 卡片浏览页三项优化（列表+单卡模式）
- 移除底部"还剩 N 张"计数：两页均删 .bottom-info CSS/HTML/JS 引用，bottom-stats 补 padding-bottom:16px 兜底留白
- 强化模式切换选中态：mode-tag 背景 0.1→0.2 透明度、字色 --primary-dark、字重 700、内描边 box-shadow
- 单卡模式新增"下一张"按钮（prev-nav 加 gap:10px 对称布局，跳过不标记，findNextPendingAfter 判定置灰）+ 正面手势导航：
  - 正面（未翻转）左滑=上一张（goPrev 撤销重做语义）、右滑=下一张（goNext 跳过）；背面手势保持标记功能不变
  - 边界反馈：frontNavDx 0.25 阻尼 + 回弹 + shake-warn 摇晃；飞出过渡 nav-out-left/right（0.4s，与 commit 动画一致）
  - 正面导航遮罩用蓝色系 front-right/front-left（与背面绿/红标记区分），refreshOverlayLabels 按 isFlipped 切换文案
  - 触摸端：touchstart 正反均记录起点，正面需水平意图判定（|dx|>8 且 >|dy|*1.2）才进入拖拽；正面轻触翻转改由 click 统一处理避免双翻
- 校验：node new Function 内联 JS 语法校验通过；bottomInfo 引用仅存于 .bak 备份
