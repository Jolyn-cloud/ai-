# CLAUDE.md（项目实例）

> 只放项目实例信息；通用方法论见 `pm-workflow` skill（AI 触发后自动加载对应环节规范）。

## 0. 项目信息

| 项 | 内容 |
|---|---|
| 项目名称 | 山东专升本小程序（统招专升本工具+课程产品），当前聚焦 **闪卡模块** |
| 客户/使用方 | 甲方（山东本地专升本培训机构，视频+手绘架构图提供需求） |
| 输入资料 | `01_需求理解.md`、`02_疑问清单.md`、`04_功能清单.md`；原始视频/截图已移出项目目录（桌面另行留存） |
| 原型文件 | `05_UI设计/闪卡演示.html`（外壳 v1.0：手机壳 iframe + 右侧逻辑说明面板）<br>`05_UI设计/` 下各独立页面（单卡 v3.0 / 列表 / 首页 / 章节统计 / 设置） |
| PRD 文件 | `03_PRD.md`（验收级 PRD：已校准模块规则唯一来源）+ 逻辑说明面板（右侧重构中）+ [04_功能清单](04_功能清单.md)（功能全景） |
| 当前版本 | 外壳 v1.0 · 单卡模式 v3.0 · 2026-08-26 |
| 一期范围一句话 | 闪卡模块（首页/单卡/列表/章节统计/设置 + 数据层 + 逻辑说明面板）；AI问答/会员/查询工具等二期待确认 |

## 1. 本项目特殊约定

### 1.1 备份策略（2026-08-26 起生效）
- **备份统一走 git，桌面不保留 `.bak-*` 副本**（PM 拍板）。
- 改文件前：先 `git add -A && git commit`（记录改动点/原因/时间，便于回溯）。
- 不再使用 `*.bak-时间戳` 文件备份；写代码前如示例流程见下方「改动前提交」。

### 1.2 原型结构
- **多页面独立**：`05_UI设计/` 下每个 html 是独立页面，均内联 `闪卡题库.js` 数据层；
- **演示外壳**：`闪卡演示.html` 左侧手机壳 iframe 套各页面 + 右侧逻辑说明面板（结构化 JS 数据渲染）；
- **版本号三处一致**：外壳页文件名版本 / 外壳顶栏 tab 页版本 / 逻辑面板版本 chip —— 需三处同步。

### 1.3 逻辑说明面板
- 右侧布局内面板（不遮挡页面），固定 460px，内容为**结构化 JS 数据 + 渲染函数**；
- 字段口径六要素：来源+取值时机 / 格式精度 / 判断阈值 / 公式 / 联动去向 / 空异常态；
- 话术写给开发，精确无歧义（≤/<、闭开区间写清楚），删产品话术。

## 2. 工作方法提醒

- 项目根**已是 git 仓库**。改任何文件前先 `git status` 看是否干净，再提交一次基线分期；
- 需求/功能/规则文档（`01~04*.md`、`03_PRD.md`）与逻辑面板内容**归属唯一**，不双份维护；
- 涉及界面改动过「去 AI 味自查清单」（配色守惯例、动效有理由、文案说人话、空异常态得体）。

## 3. 需求变更记录（走 `pm-workflow` 06 环）

| 时间 | 改动 | 原因 |
|---|---|---|
| 2026-08-26 | 删除 13 个 `.bak` 备份；初始化 git（基线提交 49af075） | PM 拍板：备份改走 git，桌面不保留 .bak |
| 2026-08-26 | 新建 `闪卡演示.html` 外壳 v1.0（手机壳+右侧逻辑面板框架） | 待写单卡模式逻辑说明 |
| 2026-08-29 | `AI伴学_小程序.html` STATE 完全冷启动（不再读 localStorage，刷新即全重置）；VIP 权益改为**仅首次进题库页**自动弹（由切任一功能 tab 弹改为仅切题库）；PRD §四（五）+逻辑面板同步 | PM 拍板：演示每次刷新重走【填画像→领VIP→登录】全流程，刷新即重新初始化；提交 `06b6c65` |
| 2026-08-30 | 学习画像考试时间**明确为 YYYY-MM-DD**（大三 2027-03-28 / 大二 2028-03-26 / 大一 2029-03-25），修正年级映射（大三→毕业当年）；修复入学年份重构 bug（非其他场景画像卡死不完成）；`calcProfileLevel` 完整度改为院校+专业+入学年份，考试时间为派生字段 | PM 拍板：考试日期写明年-月-日，系统按每年 3 月最后周日判断；提交 `c998ca7` |